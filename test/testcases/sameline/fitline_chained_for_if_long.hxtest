{
    "wrapping": {
        "maxLineLength": 80
    },
    "sameLine": {
        "forBody": "fitLine",
        "ifBody": "fitLine"
    }
}

---

class Main {
	function test() {
		for (session in lockForSessionsPaths) if (!sessions.contains(session)) sessions.push(session);
	}
}

---

class Main {
	function test() {
		for (session in lockForSessionsPaths) if (!sessions.contains(session))
			sessions.push(session);
	}
}
