{
	"indentation": {
		"character": "tab",
		"tabWidth": 4
	},
	"whitespace": {
		"parenConfig": {
			"expressionParens": {
				"openingPolicy": "none",
				"closingPolicy": "none"
			}
		}
	}
}

---

class Main {
	function test() {
		final items:Array<RecordBucketEntity> = [];
		for (branchEntry in (clusterListContent.Buckets : Array<RecordOutputElement>))
			items.push(createItem(branchEntry));
	}
}

---

class Main {
	function test() {
		final items:Array<RecordBucketEntity> = [];
		for (branchEntry in (clusterListContent.Buckets : Array<RecordOutputElement>))
			items.push(createItem(branchEntry));
	}
}
